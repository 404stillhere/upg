

# STRICT PATCH INSTRUCTION v666_5.2.2

You are performing a controlled surgical patch on an existing specification document.

You will receive the full text of:

`UNIVERSAL PROMPT GENERATOR v666_5.2.1`

Your task is to apply **only** the explicitly listed edits below.

## Non-negotiable editing rules

- Do **not** rewrite for style.
- Do **not** modernize wording.
- Do **not** improve clarity beyond the exact inserted or replaced lines below.
- Do **not** reorganize sections.
- Do **not** rename headings.
- Do **not** normalize punctuation.
- Do **not** deduplicate repeated ideas.
- Do **not** shorten anything.
- Do **not** expand anything except the exact patch insertions below.
- Do **not** fix any other perceived issues.
- Do **not** add commentary, explanations, notes, or patch summaries.
- Do **not** omit any original text.
- Do **not** output a diff.
- Output the **full final patched document only**.

If any part of the source text appears inconsistent, leave it unchanged unless one of the explicit patches below targets it.

If an insertion point appears ambiguous, choose the most literal local match and make the minimum possible edit.

---

## Patch 1A — TRACE Setting semantics for Module A

**Find this exact section heading:**
`### TRACE = true`

Within that section, find the paragraph ending with this exact sentence:

`If you cannot produce a CHECK that meets these conditions, rewrite the slot silently until you can. The reader must never see a CHECK that reports failure.`

**Insert immediately after that paragraph:**

`For Module A flat-poster workflows, Setting refers to the depicted setting inside the poster artwork, not the poster's physical presentation surface.`

Do not alter any other TRACE wording.

---

## Patch 1B — TRACE Shared props exclusion

Still within the `### TRACE = true` section, find this exact line:

`Shared props with previous slots: [list or NONE — core subject excluded]`

**Replace that line only** with:

`Shared props with previous slots: [list or NONE — core subject excluded for single-subject themes; repeated structural elements required by an active module also excluded]`

Do not alter the surrounding CHECK template.

---

## Patch 2 — Feedback / Decision Block sync

**Find this exact section heading:**
`## FEEDBACK ADJUSTMENT PROTOCOL`

Within that section, find this exact sentence:

`Preserve Decision Block and all untouched slots unchanged.`

**Replace that sentence only** with:

`Preserve Decision Block unchanged unless the feedback directly changes surfaced planning fields such as Entry Angle, lens assignment, palette summary, module emphasis, or slot plan. Preserve all untouched slots unchanged.`

Do not change any other feedback rules.

---

## Patch 3 — Entry Angle Rotation default activation

**Find this exact section heading:**
`### A.4 Creative Lenses`

Within that section, find the subsection label:

`**CONVERSATION TRACKING (lens sequences + entry angles):**`

Within that subsection, find this exact sentence:

`Reset to Theme A if user explicitly restarts the workflow.`

**Insert immediately after that sentence:**

`The Entry Angle Rotation Rule is active by default within a session and resets to Theme A only when the user explicitly restarts the workflow or a new session begins.`

Do not change any other conversation-tracking wording.

---

## Patch 4 — Module A arc-depth clarification

**Find this exact section heading:**
`### Module A — Layout & Text`

Within that section, find the paragraph that begins with:

`**MODULE A + MODULE D PRIORITY:**`

and ends with:

`If the two modules overlap, Module A determines presentation format; Module D determines what changes within the depicted content across the series.`

**Insert immediately after that paragraph:**

`**MODULE A ARC-DEPTH INTERACTION:** When Module A is active, its poster-specific slot mapping controls the visual presentation of Slots 1–5. Arc depth and active modules still govern narrative progression, legibility, and any applicable conversion or drift limits.`

Do not change any other Module A wording.

---

## Patch 5 — Module B compact minimums in detail slots

**Find this exact section heading:**
`### Module B — Package Essence`

Within that section, find this exact sentence:

`If any of the four minimums is absent, rewrite the slot before output.`

**Insert immediately after that sentence:**

`In Module B detail-oriented slots, required packaging minimums may be satisfied compactly rather than with equal descriptive emphasis.`

Do not change any other Module B wording.

---

## Patch 6 — Version label update

**Find the first line of the document:**

`# UNIVERSAL PROMPT GENERATOR v666_5.2.1`

**Replace it only** with:

`# UNIVERSAL PROMPT GENERATOR v666_5.2.2`

Do not change any other version references unless explicitly listed above.

---

## Output constraints

Before outputting, verify silently:

- all six patches were applied
- no other text was changed
- the document is complete
- no section was removed
- no headings were renamed
- no explanatory text was added

Then output:

- the complete patched document only
- no preface
- no postscript
- no notes
- no markdown fence
- no patch report

---
