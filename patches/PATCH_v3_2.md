You are a precise document editor. You will make SURGICAL edits to the
"Universal Prompt Generator v3.2" document I provide after this instruction.
Make ONLY the four changes listed below. Preserve all other sections,
formatting, markdown, code blocks, headers, and numbering exactly as-is.
Output the complete updated document.

═══════════════════════════════════════════════════════
CHANGE 1 — STEP 1c: Neutralize auto-placeholder generation
═══════════════════════════════════════════════════════

FIND this block inside Step 1c:

  PLACEHOLDERS (auto-generated):
  HEADLINE: "..."
  SLOGAN: "..."
  CTA: "..."
  BRAND: "..."

REPLACE with:

  PLACEHOLDERS (auto-generated — format labels only):
  HEADLINE: "[HEADLINE TEXT]"
  SLOGAN: "[SLOGAN TEXT]"
  CTA: "[CTA TEXT]"
  BRAND: "[BRAND NAME]"

  ⚠️ PLACEHOLDER RULE: Auto-generated placeholder strings are format-neutral
  labels. They carry no thematic, geographic, or emotional meaning and must NOT
  influence scene direction, archetype selection, or Literal Pole definition in
  Steps 2–6. Visual output is derived exclusively from THEME, never from
  placeholder text.

═══════════════════════════════════════════════════════
CHANGE 2 — INSERT new STEP 2.0 before existing Step 2a
═══════════════════════════════════════════════════════

FIND the line:

  **2a. Density check:**

INSERT the following block immediately BEFORE it (preserve the existing 2a
section in full):

---

## STEP 2.0 — PRE-ENRICHMENT DIVERGENCE PROTOCOL

**Run before Step 2a. Establishes a non-canonical visual entry point for
this generation run.**

### 2.0a — Canonical Blacklist

List the 3 most statistically probable, default, or visually clichéd
representations of THEME — the images that would appear in the first row
of a stock photo search for this exact topic.

- `CLICHÉ 1: [description of most obvious visual representation]`
- `CLICHÉ 2: [description of second most obvious]`
- `CLICHÉ 3: [description of third most obvious]`

**Rule:** These are BANNED as Literal Pole (Step 3c) and as Slot 1–2
foundations. They may appear in Slots 4–5 only in fully subverted,
deconstructed, or inverted form — never as grounding anchors.

### 2.0b — Entry Angle Selection

Select ONE non-default angle that makes CLICHÉ 1 impossible as Slot 1
output. Choose the most generative option for this theme:

- **Geographic** — a non-default location type (for travel: arctic
  wilderness, high-altitude desert, industrial port city — NOT tropical
  beach)
- **Temporal** — a non-default time marker (overcast pre-dawn, deep
  winter, off-season, midday glare — NOT golden hour by default)
- **Cultural** — a specific cultural or historical context that reframes
  the theme
- **Conceptual** — an abstract or philosophical lens (for travel: arrival
  as disorientation, not triumph; departure as grief, not excitement)
- **Sensory** — a non-visual primary sense as the scene's governing logic
  (sound, texture, smell, cold, pressure)

Declare:
`ENTRY ANGLE: [category] — [specific angle chosen]`
`REASON: [one sentence explaining why this makes the Canonical Blacklist
output impossible]`

This declaration is binding for:
- Step 2b enrichment (Pattern selection must be consistent with ENTRY
  ANGLE, not with Canonical Blacklist items)
- Step 3c Literal Pole (must be grounded in ENTRY ANGLE)
- Step 3e Seeds (must not replicate Canonical Blacklist logic)

---

═══════════════════════════════════════════════════════
CHANGE 3 — STEP 3e: Add divergence check after seeds
═══════════════════════════════════════════════════════

FIND the line at the end of Step 3e:

  Seeds 2 and 3 feed Slots 4–5 directly.

REPLACE with:

  Seeds 2 and 3 feed Slots 4–5 directly.

  **Divergence check:**
  Compare all three seeds against CANONICAL BLACKLIST (Step 2.0a).
  - Any seed visually equivalent to a blacklisted cliché → replace with a
    variation grounded in ENTRY ANGLE.
  - Declare: `DIVERGENCE CHECK: PASS` or `DIVERGENCE CHECK: ADJUSTED —
    Seed [N] replaced: [reason]`

═══════════════════════════════════════════════════════
CHANGE 4 — STEP 6: Add run fingerprint to BRIEF ANALYSIS
═══════════════════════════════════════════════════════

FIND this block in Step 6:

  Theme:    [subject + key objects]
  Goal:     [medium + emotional register + reason]
  Spectrum: [Literal Pole] → [AXIS] → [Unexpected Pole]
  Mode:     [STANDARD / NARRATIVE]
  Seeds:    [seed 1] | [seed 2] | [seed 3]
  Text:     [EMBEDDED: declared strings / HYBRID: embedded ← → deferred
             split / DEFERRED: reason / N/A]

REPLACE with:

  Theme:        [subject + key objects]
  Goal:         [medium + emotional register + reason]
  Spectrum:     [Literal Pole] → [AXIS] → [Unexpected Pole]
  Mode:         [STANDARD / NARRATIVE]
  Seeds:        [seed 1] | [seed 2] | [seed 3]
  Text:         [EMBEDDED: declared strings / HYBRID: embedded ← → deferred
                 split / DEFERRED: reason / N/A]
  Entry Angle:  [from Step 2.0b — category + specific angle]
  Fingerprint:  [3 words uniquely identifying THIS run's visual world —
                 not the theme, the specific angle chosen]
  Blacklist:    [cliché 1] | [cliché 2] | [cliché 3]